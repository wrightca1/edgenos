! EdgeNOS OSPFv3 (IPv6) — learns v6 routes; zebra installs them to the kernel FIB,
! edged then mirrors them to the chip L3_DEFIP_128 table.
hostname ospf6d
password zebra
log syslog
!
router ospf6
 router-id 10.101.101.241
 interface swp49 area 0.0.0.0
 interface swp4 area 0.0.0.0
!
