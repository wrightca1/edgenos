# EdgeNOS L3 config applied at boot by edgenos-l3.service (same format as the AS4610):
#   <iface> <cidr> [mtu]
# Front-panel ports are ge0..geN-1 (PCI order), management is ma1 (DHCP by default).
# Leave empty to configure everything through Quagga (vtysh) instead.
#ge0 10.0.0.1/24 1500
#ge1 10.0.1.1/24
