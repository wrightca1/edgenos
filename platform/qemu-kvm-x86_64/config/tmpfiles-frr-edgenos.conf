# EdgeNOS: /run/frr must be traversable so frrvty-group users (admin) can reach the vty sockets.
# frrcommon.sh only creates it (0750) when missing; the daemons' .vty sockets stay frr:frrvty 0660.
d /run/frr 0755 frr frr -
