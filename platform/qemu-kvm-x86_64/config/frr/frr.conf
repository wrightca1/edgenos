! EdgeNOS virtual switch — FRR integrated config (/etc/frr/frr.conf).
! Replaced by the per-node startup config when one is pushed (/etc/edgenos/startup/frr.conf).
frr defaults datacenter
hostname edgenos
log syslog informational
service integrated-vtysh-config
!
line vty
!
