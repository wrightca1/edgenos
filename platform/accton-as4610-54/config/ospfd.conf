hostname as4610-ospfd
password zebra
! Default image: no site OSPF (front-panel ports unaddressed). See ../deploy/ospfd.conf
! for our site config. Add a router-id + networks per deployment once interfaces are up.
