# pkgtool/ — the EdgeNOS package system (.epk) (Phase 2)

Builds arch/ASIC-tagged .epk packages (manifest.json + data.tar.zst) and provides
the on-box `epkg` installer for runtime-installable overlay packages. See DESIGN.md.
