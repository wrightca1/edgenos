# asic/<asic>/ — per-switch-ASIC support

SDK glue (OpenMDK / OpenBCM), port logic, and PHY/SerDes drivers, keyed by the `id`
in `switchdb/asic/<asic>.yml`. Add a dir here when introducing a new switch chip.
